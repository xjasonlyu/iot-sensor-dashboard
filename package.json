{
  "name": "iot-sensor-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "api:generate": "npm --prefix packages/api-contract run generate",
    "backend:typecheck": "npm --prefix backend run typecheck",
    "frontend:typecheck": "npm --prefix frontend run typecheck",
    "backend:build": "npm --prefix backend run build",
    "frontend:build": "npm --prefix frontend run build",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:build": "npm --prefix packages/api-contract run build && npm --prefix frontend run build -- --mode e2e",
    "e2e:serve": "npm run e2e:build && npm --prefix frontend run preview -- --host 127.0.0.1 --port 4173 --strictPort"
  },
  "devDependencies": {
    "@playwright/test": "1.59.1"
  }
}

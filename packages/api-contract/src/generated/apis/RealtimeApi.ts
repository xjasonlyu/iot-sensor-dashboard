/* tslint:disable */
/* eslint-disable */
/**
 * IoT Sensor Dashboard API
 * REST and Server-Sent Events (SSE) API for the IoT Sensor Dashboard.  Historical and summary data is exposed over JSON REST endpoints. Live sensor readings, sensor events, and activity updates are delivered through the SSE endpoint. API access tokens are expected to be issued by an external OAuth 2.0/OpenID Connect provider and validated by the backend. 
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime.js';
import {
    type ApiError,
    ApiErrorFromJSON,
    ApiErrorToJSON,
} from '../models/ApiError.js';

export interface StreamRealtimeEventsRequest {
    networkId: number;
    lastEventID?: string;
}

/**
 * 
 */
export class RealtimeApi extends runtime.BaseAPI {

    /**
     * Creates request options for streamRealtimeEvents without sending the request
     */
    async streamRealtimeEventsRequestOpts(requestParameters: StreamRealtimeEventsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['networkId'] == null) {
            throw new runtime.RequiredError(
                'networkId',
                'Required parameter "networkId" was null or undefined when calling streamRealtimeEvents().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['networkId'] != null) {
            queryParameters['networkId'] = requestParameters['networkId'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (requestParameters['lastEventID'] != null) {
            headerParameters['Last-Event-ID'] = String(requestParameters['lastEventID']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/realtime/events`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Opens a long-lived SSE connection for the selected network.  The server emits named events: `sensor.reading`, `sensor.event`, `activity.updated`, and `heartbeat`. Each `data` field is a JSON-encoded object described by the corresponding realtime schema.  The server sends an `id` with data events. Native `EventSource` automatically reconnects and sends the most recent ID in the `Last-Event-ID` header, allowing the backend to replay a small missed-event buffer. Heartbeats should be sent periodically so intermediaries do not close an otherwise idle connection.  Native browser `EventSource` cannot set an `Authorization` header, so the frontend consumes this stream with an authenticated fetch-based SSE client. The client is responsible for reconnecting with the latest access token and `Last-Event-ID`. Do not put a long-lived OAuth access token in the query string. 
     * Stream live updates using Server-Sent Events
     */
    async streamRealtimeEventsRaw(requestParameters: StreamRealtimeEventsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<string>> {
        const requestOptions = await this.streamRealtimeEventsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        if (this.isJsonMime(response.headers.get('content-type'))) {
            return new runtime.JSONApiResponse<string>(response);
        } else {
            return new runtime.TextApiResponse(response) as any;
        }
    }

    /**
     * Opens a long-lived SSE connection for the selected network.  The server emits named events: `sensor.reading`, `sensor.event`, `activity.updated`, and `heartbeat`. Each `data` field is a JSON-encoded object described by the corresponding realtime schema.  The server sends an `id` with data events. Native `EventSource` automatically reconnects and sends the most recent ID in the `Last-Event-ID` header, allowing the backend to replay a small missed-event buffer. Heartbeats should be sent periodically so intermediaries do not close an otherwise idle connection.  Native browser `EventSource` cannot set an `Authorization` header, so the frontend consumes this stream with an authenticated fetch-based SSE client. The client is responsible for reconnecting with the latest access token and `Last-Event-ID`. Do not put a long-lived OAuth access token in the query string. 
     * Stream live updates using Server-Sent Events
     */
    async streamRealtimeEvents(requestParameters: StreamRealtimeEventsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<string> {
        const response = await this.streamRealtimeEventsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
